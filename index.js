// How to apply that in colors? Just a test to keep in mind
let background = Math.Floor(Random.Number() * 4);

switch (background){
  case 0:
  background = 'red';
  case 1:
  background = 'yellow';
  default:
  backgorund = 'black'
}
